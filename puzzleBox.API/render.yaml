services:
  - type: web
    name: puzzlebox-api
    env: dotnet
    plan: free
    region: oregon
    buildCommand: "dotnet publish -c Release -o publish"
    startCommand: "dotnet publish/puzzleBox.dll"
    envVars:
      - key: DOTNET_RUNNING_IN_CONTAINER
        value: true